name: ai_todo
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  google_generative_ai: ^0.4.6
  speech_to_text: ^7.0.0
  permission_handler: ^10.0.0
  hive_flutter: ^1.1.0
  hive: ^2.2.3  # Hive ana paketini de ekleyin

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.11
  hive_generator: ^1.1.3  # Bu satırı ekleyin

flutter:
  uses-material-design: true
